Various ZSH completion scripts. These must be located in a $fpath
directory. For example, in ~/.zshrc set:

    fpath=(~/.zsh/functions $fpath)
    autoload -U compinit && compinit

and then place whatever completions are necessary into ~/.zsh/functions,
restart any open shells, etc.
